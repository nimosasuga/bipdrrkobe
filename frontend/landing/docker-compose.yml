services:
  landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: drrkobe-landing-web
    restart: unless-stopped
    mem_limit: 128m
    cpus: 0.25
    networks:
      - proxy

networks:
  proxy:
    external: true
